name: Actions1 Workflow

on: push

jobs:
  build_job_1:
    runs-on: ubuntu-latest
    steps:
      - name: Install Cowsay
        run: sudo apt-get install cowsay -y

      - name: Run Cowsay for Dragon
        run: cowsay -f dragon "Dragon check......dscdww" >> dragon.txt
      
      - name: Run Cowsay for Vader
        run: cowsay -f vader "The Force is strong with this one." >> vader.txt

      - name: Upload Artifacts
        uses: actions/upload-artifact@v7
        with:
          name: dragon-artifact
          path: |
            dragon.txt
            vader.txt

  test_job_2:
    needs: build_job_1
    runs-on: ubuntu-latest
    steps: 
      - name: Download Artifact
        uses: actions/download-artifact@v8
        with:
          name: dragon-artifact

      - name: Check dragon
        run: grep -i "dragon" dragon.txt

      - name: Check vader
        run: grep -i "force" vader.txt
    
  deploy_job_3:
    needs: [build_job_1, test_job_2]
    runs-on: ubuntu-latest
    steps:
      - name: Download Artifact
        uses: actions/download-artifact@v8
        with:
          name: dragon-artifact

      - name: Read File Dragon
        run: cat dragon.txt

      - name: Read File Vader
        run: cat vader.txt
      
      - name: Deploy File
        run: echo Deploying.......




    # ascii_job:
    #     runs-on: ubuntu-latest
    #     steps:
    #         - name: Add Checkout Repo
    #           uses: actions/checkout@v6

    #         - name: List and Read Files
    #           run: |
    #             echo "Actions Test Run"
    #             ls -ltra
    #             cat README.md
            
    #         - name: Execute Shell Script
    #           run: |
    #             chmod +x ascii-script.sh
    #             ./ascii-script.sh
